{
  "name": "the-ledger-back",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.74.0"
  }
}
